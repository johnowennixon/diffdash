Configuration file that defines consistent coding styles across different editors and IDEs. Specifies UTF-8 character encoding, Unix-style line endings (LF), 2-space indentation, and automatic insertion of a final newline at the end of each file. Also enforces removal of trailing whitespace to maintain clean code files across the codebase.
