Configuration file for the `ack` command-line search tool that specifies directories and files to exclude from search results. Instructs ack to ignore the `out` directory which likely contains build artifacts. Also configures ack to skip files with the `.lock` extension, which are typically automatically generated dependency lock files that don't contain relevant searchable code.
