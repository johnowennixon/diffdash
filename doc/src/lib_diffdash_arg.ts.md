Defines the command-line argument schema and parser for the DiffDash tool, which generates Git commit messages using AI. The module exports an argument schema that includes options for version display, model comparison, commit message customization, automated Git operations, workflow control flags, and LLM configuration settings. It provides a complete argument parser configured with a description and all available command-line options for the DiffDash application.
