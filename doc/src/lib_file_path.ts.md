Provides essential file path manipulation utilities for working with file and directory paths. Wraps Node.js path module functions to handle common operations like joining path segments, extracting file names, and converting between relative and absolute paths. Simplifies cross-platform path handling without requiring direct interaction with the underlying Node.js API.
