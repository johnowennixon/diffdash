Defines a system for creating and parsing command-line interfaces with various argument types such as strings, integers, booleans, choices, lists, and nested groups. Supports building argument schemas, adding them to parsers, and parsing user input into typed results. Includes utilities for handling argument options, predicates, and mutually exclusive groups.
